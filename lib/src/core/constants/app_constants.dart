/// Common spacing constants used throughout the app
class AppSpacing {
  AppSpacing._();

  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 20.0;
  static const double xxl = 24.0;
  static const double xxxl = 32.0;
  static const double huge = 40.0;
}

/// Common size constants
class AppSizes {
  AppSizes._();

  // Border radius
  static const double radiusSm = 4.0;
  static const double radiusMd = 8.0;
  static const double radiusLg = 10.0;
  static const double radiusXl = 12.0;
  static const double radiusRound = 16.0;

  // Icon sizes
  static const double iconSm = 16.0;
  static const double iconMd = 24.0;

  // Border width
  static const double borderWidth = 1.0;

  // Scrollbar
  static const double scrollThumbHeight = 33.0;
  static const double scrollThumbWidth = 4.0;

  // Container heights
  static const double appBarHeight = 48.0;
  static const double tabBarHeight = 40.0;
  static const double tabBarSmHeight = 32.0;
  static const double tabBarXsHeight = 18.0;
}

/// Common animation durations
class AppDurations {
  AppDurations._();

  static const Duration fast = Duration(milliseconds: 200);
  static const Duration normal = Duration(milliseconds: 300);
  static const Duration slow = Duration(milliseconds: 500);
}
